import React from 'react';
import PublicLayoutPage from './PublicLayoutPage';

const PublicLayout = ({ children }) => {
  return (
    <PublicLayoutPage children={children} />
  );
};

export default PublicLayout